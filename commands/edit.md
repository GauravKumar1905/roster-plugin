---
description: Change a report you already have
---

Change this report: **$ARGUMENTS**

Use the `report-editor` agent. It reads the existing design, proposes the change as a difference,
previews it against real data, and saves over the original rather than creating a second report.

If nothing was named, run `list_report_templates` and ask which one.

Change only what was asked. The report is already in front of a client, and someone is used to
how it looks.
